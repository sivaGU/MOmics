# features/

Placeholder. Intended to hold the exported feature lists used by the locked pipeline:

- `RNA_features.txt`
- `protein_features.txt`
- `metabolite_features.txt`

These are currently embedded in `models/MOmics_v11_locked_pipeline.pkl` (`pruned_features` key) rather than checked in as standalone files.
