# scripts/

Placeholder for standalone CLI entry points wrapping the pipeline in `ml_pipeline.py`, e.g.:

- `run_training.py`
- `run_validation.py`
- `run_feature_selection.py`
- `generate_reports.py`

Currently, training and paper-figure generation both live in `ml_pipeline.py` at the repo root; these would be thin wrappers around it.
